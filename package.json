{
  "name": "vl53l5cx",
  "version": "0.0.1",
  "description": "I2C bindings for VL53L5CX multizone ToF sensor driver.",
  "main": "dist/index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "prepare": "node-gyp rebuild && tsc",
    "clean": "node-gyp clean",
    "debug-build": "node-gyp rebuild --debug && tsc",
    "build": "node-gyp rebuild && tsc",
    "rebuild": "node-gyp rebuild && tsc",
    "bear-build": "bear -- node-gyp rebuild && tsc",
    "bear-rebuild": "bear -- node-gyp rebuild && tsc",
    "configure": "node-gyp configure && tsc"
  },
  "keywords": [
    "VL53L5CX",
    "OpenI2C"
  ],
  "author": "",
  "license": "MIT",
  "gypfile": true,
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.18",
    "node-gyp": "^11.2.0",
    "ts-jest": "^29.3.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}